# Legacy OpenAgents Commands

This directory contains **legacy** command definitions from the OpenAgents era.

## Status: ARCHIVED

These files are **not part of the active command surface**. They are kept for historical reference only.

## Active Surface

The active commands are the **flat files** in `.opencode/command/`:

- `start-session`, `end-session`, `harness-status`, `dashboard`
- `query`, `update`, `task`, `orchestrate`, `architect`
- `debug`, `test`, `commit`, `clean`, `optimize`
- `skill-propose`, `skill-load`
- `mcp-discover`, `mcp-approve`, `mcp-load`
- And others — see `.opencode/command/` for the full list

## Legacy Files in This Directory

| File | Status | Notes |
| ---- | ------ | ----- |
| `check-context-deps.md` | Archived | OpenAgents-era context dependency checker |

## Rule

**Do not reference these files from live documentation.** The flat files in `.opencode/command/` are the source of truth.
